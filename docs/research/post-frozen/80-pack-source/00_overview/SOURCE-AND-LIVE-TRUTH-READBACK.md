# Source And Live Truth Readback

| Class | Readback |
|---|---|
| zip-derived fact | Uploaded localhost route pack contains user primary ask, codex readbacks, code refs, planning inputs, and execution evidence. |
| live GitHub fact | PR #194 is merged and adds STEP0 web GPT templates under `docs/research/web-gpt-step0/**`; these are candidate-only references. |
| live GitHub fact | `AGENTS.md` and `docs/current.md` are no longer stale at the PR #192 state; they now record Wave 6 candidate open / not execution approved. |
| live GitHub fact | `create_app()` still does not include the Bridge router. |
| live GitHub fact | H5 `UrlBar` still has a static Create capture button and does not call `/captures/discover`. |
| live GitHub fact | Vault preview/commit H5 surfaces still default to placeholder/dry-run disabled states. |
| inference | The shortest high-value next line is `PF-localhost-preview`, preceded by a thin `PF-C0/O1` entry/overflow guard. |

## Frozen boundary

Dispatch126-176 are historical evidence, not executable backlog.
