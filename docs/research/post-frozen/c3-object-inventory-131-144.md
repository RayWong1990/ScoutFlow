---
title: C3 object inventory 131-144
status: candidate / research / not-authority
created_at: 2026-05-06
related_dispatch: PF-C3-01
---

# C3 object inventory 131-144

## source_boundary

- Scope only covers `T-P1A-131 ~ T-P1A-144` as already-landed candidate surfaces in [docs/task-index.md](/Users/wanglei/workspace/ScoutFlow/docs/task-index.md:78).
- This note classifies object pressure; it does not reopen Dispatch126-176 and does not create execution approval.
- Runtime, browser automation, migration, and true vault write remain blocked.

## live_inventory_table

| task | object_focus | current_role | risk | proposed_bucket | note |
|---|---|---|---|---|---|
| `T-P1A-131` | topic card IA candidate | core preview surface | low | `keep` | closest object to near-term usefulness proof |
| `T-P1A-132` | capture plan IA candidate | companion preview surface | low | `keep` | keep as paired lite surface, not a second product lane |
| `T-P1A-133` | hypothesis comparison UX | explanatory layer | medium | `compress` | keep idea, reduce standalone object weight |
| `T-P1A-134` | signal ingestion audit lane | audit-only seam | medium | `overflow` | useful as audit evidence, not near-term surface |
| `T-P1A-135` | visual reporting candidate | support evidence | medium | `compress` | fold into proof packet instead of separate object |
| `T-P1A-136` | localhost review roster | review support | low | `keep` | needed to map who reviews which preview proof |
| `T-P1A-137` | STEP3 commander prompt note | process support | medium | `overflow` | reusable later, not part of minimal object set |
| `T-P1A-138` | cloud draft resume rules | handoff support | medium | `overflow` | keep as packaging reference only |
| `T-P1A-139` | readback delta rules | truth-maintenance support | low | `keep` | helps separate introduced vs exposed drift |
| `T-P1A-140` | deferred / overflow registry | boundary control | low | `keep` | single overflow inbox is better than many loose notes |
| `T-P1A-141` | bridge hardening continuation | future hardening seam | medium | `overflow` | wait for proof reality before reopening |
| `T-P1A-142` | vault preview continuation | preview data seam | low | `keep` | still preview-only, but directly tied to proof loop |
| `T-P1A-143` | vault dry-run continuation | dry-run seam | medium | `compress` | keep as appendix under preview contract, not peer object |
| `T-P1A-144` | 5 Gate CI continuation | automation support | high | `compress` | blocked as proof source; retain only as future gate note |

## bucket_summary

| bucket | rows | why |
|---|---:|---|
| `keep` | 6 | direct service to preview-only usefulness or boundary control |
| `compress` | 4 | concept survives, standalone object does not |
| `overflow` | 4 | future support or hardening reservoir |
| `kill` | 0 | no row needs deletion if object weight is reduced elsewhere |

## conflict_verdict

- `topic-card-lite + capture-plan-lite + preview/evidence source control` should remain the minimal visible layer.
- `scoring / lifecycle / visual / dry-run / automation` should stop presenting as peer-level first-class objects before proof depth exists.
- Verdict: `T-PASS` for inventory classification only; no downstream gate is opened by this note.
