---
title: External research queue
status: candidate / research / not-authority
created_at: 2026-05-06
related_dispatch: PF-GLOBAL-10
---

# External research queue

## queue

| comparator | why it belongs here | open_question | expected_output |
|---|---|---|---|
| `ArchiveBox` | archive-oriented lower-level reference | what is the minimum archive proof ScoutFlow actually needs? | short markdown note |
| `Karakeep` | personal knowledge capture reference | how much triage UX is enough before workbench creep starts? | matrix |
| `MyTube` | source ingestion reference | what is the thinnest ingest-to-review seam worth copying? | seam note |
| `MeTube` | lower-bound downloader reference | which parts are intentionally out of scope for ScoutFlow? | reject list |
| `MediaCrawler` | adapter/reference pattern | which boundary ideas transfer without copying runtime risk? | reference-only memo |
| `OpenDesign / shadcn-admin` | UI reuse reference | what should remain reference, not copy? | adapt/reject table |

## queue_rules

- External research is read-only until a new dispatch explicitly opens adoption work.
- Each row must end in `reference`, `adapt`, or `reject`; no silent copy default.

## verdict

- `T-PASS` for queue design only.
