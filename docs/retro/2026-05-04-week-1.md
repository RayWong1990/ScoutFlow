# Week 1 Retro — 2026-05-04

## Velocity vs Scope
- PRs merged: `PR #26` and `PR #27` were already merged before this retro; the current `07.x` branch is docs-only remediation.
- Product-code PRs / docs-governance PRs: `TODO if user wants a weekly ratio, read from Git history after this branch merges`.
- End-to-end user-visible demo: `no`
- If no, why: `T-P1A-011` no-auth probe remained blocked; `T-P1A-011` preflight repair completed; `T-P1A-011B` manual-auth QR local-only gate completed; `T-P1A-011C` auth-present metadata probe completed with `platform_result=ok`, but it still did not approve media / ffmpeg / ASR / receipt / capture state, and receipt wiring has not started yet.

## Agent Switching
- Main agents used: `Codex Desktop` as single writer; external prompt pack and probe reports were read as side inputs.
- Approximate handoffs: `TODO user-only if a subjective summary is needed`.
- Single Writer / Multi Reviewer conflict: `no known conflict`
- State drift observed: pre-08 prompt premises still pointed at blocked `011`, so a second retro plus prompt patch was needed before the next code-bearing task.

## Scope Drift
- New amendments/specs/research notes: `T-P1A-011` blocked probe report, `T-P1A-011B` local-only auth gate report, `T-P1A-011C` auth-present probe report, and this `T-P1A-011D` second retro triage.
- Any outline to postpone/shrink: `07.5` legal/vendor notes stay optional; any 19-file remediation package stays out; velocity gate does not block 08.
- Any task to split: `T-P1A-012R` receipt-wiring retro moves after Dispatch 08 instead of before it.

## Lesson
- Saved time: `011C` produced safe auth-present metadata evidence, so patched Dispatch 08 can consume existing evidence instead of re-running BBDown.
- Wasted time: blocked no-auth assumptions and stale 08/09 prompt premises created avoidable drift.

## Next Focus
- One sentence: patched Dispatch 08 should consume existing `T-P1A-011C` evidence only.
