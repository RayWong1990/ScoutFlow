# Scheme C GitHub Queue Smoke

This directory contains a minimal smoke flow for scheme C:

1. create a real GitHub issue with a task marker
2. run a local worker that claims the issue
3. write completion back as GitHub issue comments

It is intentionally scoped away from ScoutFlow product code.
